#lang sicp
(define size 2) ; simple variable and value
;(display(* 5 size)) combination of variable and value expressions

(define pi 3.14159)
(define radius 10)
(define circumference (* 2 pi radius))
(display circumference) ; complex define process